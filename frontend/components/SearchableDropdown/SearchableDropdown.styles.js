import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    selected: {
        flexDirection: 'row',
        alignItems: 'center',
        padding: 8,
    },

});

export default styles;
